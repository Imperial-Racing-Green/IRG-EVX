sze = size(quarternion_out.Data);
camber = zeros(sze(1),1);
toe = zeros(sze(1),1);
rot = zeros(sze(1),1);

for i = 1:sze(1)
    [camber(i), toe(i), rot(i)] = quat2angle(quarternion_out.Data(i,:));
end
camber = camber.*57.2958;
toe = toe.*57.2958;
plot(quarternion_out.Time,camber)
hold on
plot(quarternion_out.Time,toe)
plot(quarternion_out.Time,rot)
